{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "install": {
      "commands": ["pnpm install"]
    },
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": ["cd ../.. && npx turbo run build"]
    }
  },
  "deploy": {
    "startCommand": "node dist/index.js"
  }
}
